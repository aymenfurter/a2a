# =============================================================================
# REQUIRED: Azure AI Project Configuration
# =============================================================================
# Get these from your Azure AI Studio project
PROJECT_ENDPOINT=https://your-project.cognitiveservices.azure.com/
MODEL_DEPLOYMENT_NAME=gpt-4

# =============================================================================
# REQUIRED: Confluence Configuration for MCP server
# =============================================================================
# Your Confluence instance URL (without /wiki path)
CONFLUENCE_URL=https://your-company.atlassian.net

# Your Confluence username (usually your email)
CONFLUENCE_USERNAME=your.email@company.com

# Generate API token at: https://id.atlassian.com/manage-profile/security/api-tokens
CONFLUENCE_API_TOKEN=your-confluence-api-token

# =============================================================================
# OPTIONAL: Jira Configuration (if using both Confluence and Jira)
# =============================================================================
JIRA_URL=https://your-company.atlassian.net
JIRA_USERNAME=your.email@company.com
JIRA_API_TOKEN=your-jira-api-token

# =============================================================================
# MCP Server Configuration
# =============================================================================
# IMPORTANT: The MCP server URL must be accessible from Azure AI services
# 
# For local development, consider using:
# 1. ngrok: ngrok http 9001  (then use the ngrok URL)
# 2. Azure Container Instances to host your MCP server
# 3. A cloud-hosted MCP server
#
# Default localhost (will create agent without MCP tools):
MCP_SERVER_URL=http://localhost:9001/mcp
MCP_SERVER_LABEL=atlassian

# Example with ngrok (replace with your actual ngrok URL):
# MCP_SERVER_URL=https://abc123.ngrok.io/mcp
# MCP_SERVER_LABEL=atlassian

# Example with cloud-hosted server:
# MCP_SERVER_URL=https://your-mcp-server.azurecontainer.io/mcp
# MCP_SERVER_LABEL=atlassian

# =============================================================================
# OPTIONAL: Additional Configuration
# =============================================================================
# Filter specific Confluence spaces (comma-separated)
CONFLUENCE_SPACES_FILTER=DEV,TEAM,DOC

# Enable read-only mode (prevents creating/updating pages)
READ_ONLY_MODE=false

# Enable verbose logging
MCP_VERBOSE=true
MCP_LOGGING_STDOUT=true

# =============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual configuration
# 3. Ensure you have Azure authentication set up (az login or service principal)
# 4. Start the MCP server before running this agent
# =============================================================================
